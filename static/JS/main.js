var evento = document.getElementById(enviar_arquivo);

function evento_arqu (){
   InputEvent = evento;
   return InputEvent = <input class="button_enviar" type="submit" value="Enviar arquivo"></input>;
}